spring:
  datasource:
    url: jdbc:h2:mem:testdb      # Temporary in-memory DB
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console